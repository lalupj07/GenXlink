[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "genxlink-server"
port = 8080

[services.healthcheck]
path = "/health"
interval = 30
timeout = 10
